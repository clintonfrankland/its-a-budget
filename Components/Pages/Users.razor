@page "/users"
@rendermode InteractiveServer
@inject NavigationManager Navigation

<PageTitle>Users | It's a Budget</PageTitle>

@code {
    protected override void OnInitialized()
    {
        // Redirect to Settings page users section
        Navigation.NavigateTo("/settings#users", replace: true);
    }
}
